import 'package:flutter/material.dart';

class SaveDashBoardBusiness extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Save"),
      ),
    );
  }
}